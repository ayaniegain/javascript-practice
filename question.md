# JavaScript Practice Questions

## Basic Level

1. Write a function that returns the largest of two numbers.
2. Write a function that checks whether a given number is even or odd.
3. Write a function that converts Celsius to Fahrenheit.
4. Write a function that takes an array of numbers and returns the sum of all numbers.
5. Write a function that takes a string and returns the reverse of the string.
6. Write a function that takes an array of integers and returns the average (mean) of all the elements.
7. Write a function that takes an array of numbers and returns the numbers that are even.
8. Write a function that checks whether a given string is a palindrome.
9. Write a function that takes two arrays and returns a new array with unique elements from both arrays.
10. Write a function that takes a number and returns true if the number is prime, false otherwise.

## Intermediate Level

11. Write a function that sorts an array of integers in ascending order (without using the built-in `sort` method).
12. Write a function that implements a binary search algorithm.
13. Write a function that finds the maximum consecutive sum in an array of numbers (Kadane's Algorithm).
14. Implement a function that flattens nested arrays.
15. Write a function that implements a basic calculator (addition, subtraction, multiplication, division).
16. Create a class `Person` with properties `name` and `age`, and a method `greet()` that prints a greeting with the person's name.
17. Create a class `Rectangle` with properties `width` and `height`, and methods `area()` and `perimeter()`.
18. Implement a class `Stack` with methods `push`, `pop`, and `isEmpty`.
19. Create a class `BankAccount` with properties `balance` and methods `deposit()` and `withdraw()`.
20. Implement inheritance in JavaScript with a base class and a derived class demonstrating method overriding.

## Advanced Level

21. Write a function that simulates asynchronous behavior using `setTimeout()` and a callback.
22. Write a function that makes an asynchronous HTTP request using `fetch` and handles the response using Promises.
23. Implement a function that chains multiple asynchronous operations using `async/await`.
24. Write a function that toggles the background color of a DOM element on click.
25. Implement a function that fetches data from an API and dynamically creates HTML elements to display the data.
26. Implement a function using arrow functions and template literals.
27. Write a function that uses destructuring to extract values from objects or arrays.
28. Implement a function that uses `Map` or `Set` to manage unique elements.
29. Write unit tests for a simple JavaScript function using a testing framework like Jest or Mocha.
30. Build a small web application (e.g., a to-do list, weather app) using HTML, CSS, and JavaScript, focusing on functionality and user interaction.

## Arrays

31. Find the maximum and minimum element in an array.
32. Calculate the sum of all elements in an array.
33. Reverse an array.
34. Rotate an array by k elements.
35. Find the duplicate element in an array.
36. Find the missing number in a sequence.
37. Determine if two arrays are equal or contain the same elements (ignoring order).
38. Remove duplicates from a sorted array.
39. Merge two sorted arrays into one sorted array.
40. Find the intersection of two arrays.

## Strings

41. Reverse a string.
42. Check if a string is palindrome.
43. Check if two strings are anagrams of each other.
44. Count the occurrences of a character in a string.
45. Find the longest substring without repeating characters.
46. Check if a string contains only digits.
47. Convert a string to an integer (atoi).
48. Capitalize the first letter of each word in a sentence.
49. Remove whitespace from a string.
50. Check if a string is a valid palindrome considering only alphanumeric characters and ignoring cases.

## Numbers

51. Check if a number is prime.
52. Find the factorial of a number.
53. Calculate the Fibonacci sequence up to a certain number of terms.
54. Check if a number is even or odd.
55. Determine if a number is a palindrome.
56. Find the nth term of the Fibonacci sequence.
57. Check if a number is a power of two.
58. Compute the square root of a number without using built-in functions.
59. Find the largest prime factor of a number.
60. Calculate the sum of digits of a number.

## Sorting

61. Implement bubble sort, selection sort, and insertion sort algorithms.
62. Implement merge sort and quicksort algorithms.
63. Sort an array of 0s, 1s, and 2s (Dutch National Flag problem).
64. Sort an array of strings based on their lengths.
65. Sort an array of integers in ascending order, followed by negative integers in descending order.
66. Implement a stable sorting algorithm.
67. Sort a linked list using merge sort or insertion sort.

## Searching

68. Implement linear search and binary search algorithms.
69. Find the first occurrence of an element in a sorted array.
70. Find the last occurrence of an element in a sorted array.
71. Search in a rotated sorted array.
72. Find the peak element in an array.
73. Search in a 2D matrix.
74. Find the missing number in a sorted array of consecutive integers.
75. Find the majority element in an array.

## Object-oriented Programming (OOP)

76. Create a class `Car` with properties `make`, `model`, and `year`, and a method `displayInfo()`.
77. Create a class `Animal` with properties `name` and `species`, and a method `makeSound()`.
78. Implement a class `Queue` with methods `enqueue`, `dequeue`, and `isEmpty`.
79. Create a class `Library` with properties `books` (an array) and methods `addBook()` and `removeBook()`.
80. Implement polymorphism in JavaScript with a base class and multiple derived classes.

## Asynchronous Programming

81. Write a function that simulates a delay using `setTimeout()` and returns a Promise.
82. Write a function that makes multiple asynchronous HTTP requests and handles the responses using Promises.
83. Implement a function that retries an asynchronous operation a specified number of times using `async/await`.
84. Write a function that fetches data from multiple APIs and combines the results.
85. Implement a function that handles errors in asynchronous operations using `try/catch`.

## DOM Manipulation

86. Write a function that adds a new item to a list when a button is clicked.
87. Implement a function that removes an item from a list when a button is clicked.
88. Write a function that changes the text content of a DOM element when a button is clicked.
89. Implement a function that toggles the visibility of a DOM element when a button is clicked.
90. Write a function that updates the content of a DOM element with data fetched from an API.

## ES6 and Modern JavaScript

91. Implement a function using default parameters.
92. Write a function that uses the rest operator to accept an arbitrary number of arguments.
93. Implement a function that uses the spread operator to merge arrays.
94. Write a function that uses template literals to create a string with embedded expressions.
95. Implement a function that uses destructuring to swap the values of two variables.

## Additional Questions

96. What is JavaScript?
97. What is asynchronous programming in JavaScript?
98. What are the different types of data types in JavaScript?
99. What is the difference between `null` and `undefined`?
100. What are the different types of errors in JavaScript?
101. Why use `"use strict"`?
102. What will the code below output? Explain your answer.
    ```javascript
    console.log(0.1 + 0.2);
    console.log(0.1 + 0.2 == 0.3);
    ```
103. What are first-class functions?
104. What is a unary function?
105. What is the Temporal Dead Zone (TDZ)?