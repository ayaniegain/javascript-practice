- **Arrays:**
  - Find the maximum and minimum element in an array.
  - Calculate the sum of all elements in an array.
  - Reverse an array.
  - Rotate an array by k elements.
  - Find the duplicate element in an array.
  - Find the missing number in a sequence.
  - Determine if two arrays are equal or contain the same elements (ignoring order).
  - Remove duplicates from a sorted array.
  - Merge two sorted arrays into one sorted array.
  - Find the intersection of two arrays.
- **Strings:**
  - Reverse a string.
  - Check if a string is palindrome.
  - Check if two strings are anagrams of each other.
  - Count the occurrences of a character in a string.
  - Find the longest substring without repeating characters.
  - Check if a string contains only digits.
  - Convert a string to an integer (atoi).
  - Capitalize the first letter of each word in a sentence.
  - Remove whitespace from a string.
  - Check if a string is a valid palindrome considering only alphanumeric characters and ignoring cases.
- **Numbers:**
  - Check if a number is prime.
  - Find the factorial of a number.
  - Calculate the Fibonacci sequence up to a certain number of terms.
  - Check if a number is even or odd.
  - Determine if a number is a palindrome.
  - Find the nth term of the Fibonacci sequence.
  - Check if a number is a power of two.
  - Compute the square root of a number without using built-in functions.
  - Find the largest prime factor of a number.
  - Calculate the sum of digits of a number.
- **Sorting:**
  - Implement bubble sort, selection sort, and insertion sort algorithms.
  - Implement merge sort and quicksort algorithms.
  - Sort an array of 0s, 1s, and 2s (Dutch National Flag problem).
  - Sort an array of strings based on their lengths.
  - Sort an array of integers in ascending order, followed by negative integers in descending order.
  - Implement a stable sorting algorithm.
  - Sort a linked list using merge sort or insertion sort.
- **Searching:**
  - Implement linear search and binary search algorithms.
  - Find the first occurrence of an element in a sorted array.
  - Find the last occurrence of an element in a sorted array.
  - Search in a rotated sorted array.
  - Find the peak element in an array.
  - Search in a 2D matrix.
  - Find the missing number in a sorted array of consecutive integers.
  - Find the majority element in an array.